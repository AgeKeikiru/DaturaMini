// 2021-06-29 20:10:07
#event properties (no comments/etc. here are saved)
parent_index = objA_pAtk;
sprite_index = spr_explosion;
uses_physics = false;

#event create
event_inherited();

duration = sprite_get_number(sprite_index);
dmg = 1;

slowTo = SLOW_STD;

shakeX = [0, 0];
shakeY = [0, 0];

shakeCam(random_range(-3,3), random_range(-3,3));